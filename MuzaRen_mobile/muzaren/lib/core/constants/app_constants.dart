enum BookingStatus { pending, accepted, completed, cancelled }
enum ListingStatus { active, paused, expired }
enum VerificationStatus { unverified, pending, verified }
enum ReportCategory { spam, inappropriate, scam, other }
enum ReportTargetType { listing, user, review }
